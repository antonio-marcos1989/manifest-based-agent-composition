# Spring Data Mongo instancia tipos embutidos via construtor; sem nomes de parâmetro no bytecode
# isso gera "Parameter ... does not have a name". @ConstructorProperties nos construtores Lombok
# permite o mapeamento mesmo quando o compilador do IDE não usa -parameters.
lombok.anyConstructor.addConstructorProperties=true
